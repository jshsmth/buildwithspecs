---
// FeaturesTiled - Tiled feature grid section inspired by neobrutalist design
---

<section class="section features-tiled" id="what-is-sdd">
	<div class="container">
		<div class="section-header">
			<h2>What is Spec-Driven Development?</h2>
			<p class="section-intro">
				A software methodology that prioritizes planning and documentation <em>before</em> writing code. Instead of diving straight into implementation, you create a comprehensive specification that guides every aspect of development.
			</p>
		</div>

		<div class="features-grid">
			<!-- Feature 1: Top-left - Large -->
			<div class="feature-tile large tile-purple">
				<div class="tile-icon">
					<div class="icon-circle">
						<svg width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
							<polyline points="14 2 14 8 20 8"></polyline>
							<line x1="16" y1="13" x2="8" y2="13"></line>
							<line x1="16" y1="17" x2="8" y2="17"></line>
							<polyline points="10 9 9 9 8 9"></polyline>
						</svg>
					</div>
				</div>
				<div class="tile-content">
					<h3>Structured Specifications</h3>
					<p>Transform vague ideas into crystal-clear requirements that AI can execute flawlessly.</p>
					<ul class="feature-list">
						<li>→ Requirements phase</li>
						<li>→ Design documentation</li>
						<li>→ Task breakdown</li>
						<li>→ Implementation</li>
					</ul>
				</div>
			</div>

			<!-- Feature 2: Top-right - Medium -->
			<div class="feature-tile medium tile-blue">
				<div class="tile-icon-small">
					<svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
						<polyline points="16 18 22 12 16 6"></polyline>
						<polyline points="8 6 2 12 8 18"></polyline>
					</svg>
				</div>
				<div class="tile-content">
					<h3>AI-Powered Workflow</h3>
					<p>Let Claude Code guide you through each phase with intelligent prompts and validation.</p>
				</div>
			</div>

			<!-- Feature 3: Middle-left - Medium -->
			<div class="feature-tile medium tile-green">
				<div class="tile-icon-small">
					<svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
						<polyline points="20 6 9 17 4 12"></polyline>
					</svg>
				</div>
				<div class="tile-content">
					<h3>Quality Gates</h3>
					<p>Built-in approval checkpoints ensure every phase meets your standards before proceeding.</p>
				</div>
			</div>

			<!-- Feature 4: Middle-right - Large -->
			<div class="feature-tile large tile-yellow">
				<div class="tile-icon">
					<div class="icon-circle">
						<svg width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<circle cx="12" cy="12" r="10"></circle>
							<polyline points="12 6 12 12 16 14"></polyline>
						</svg>
					</div>
				</div>
				<div class="tile-content">
					<h3>10x Faster Planning</h3>
					<p>What used to take days now takes minutes with AI-assisted spec generation.</p>
					<div class="stat-highlight">
						<div class="stat-number">10x</div>
						<div class="stat-label">Speed Increase</div>
					</div>
				</div>
			</div>

			<!-- Feature 5: Bottom-left - Small -->
			<div class="feature-tile small tile-pink">
				<div class="tile-content-centered">
					<div class="icon-minimal">
						<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
							<circle cx="9" cy="7" r="4"></circle>
							<path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
							<path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
						</svg>
					</div>
					<h4>Team Aligned</h4>
					<p>Single source of truth</p>
				</div>
			</div>

			<!-- Feature 6: Bottom-middle - Small -->
			<div class="feature-tile small tile-purple">
				<div class="tile-content-centered">
					<div class="icon-minimal">
						<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<path d="M12 20v-6M6 20V10M18 20V4"></path>
						</svg>
					</div>
					<h4>Less Rework</h4>
					<p>85% fewer rewrites</p>
				</div>
			</div>

			<!-- Feature 7: Bottom-right - Small -->
			<div class="feature-tile small tile-blue">
				<div class="tile-content-centered">
					<div class="icon-minimal">
						<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
							<polyline points="22 4 12 14.01 9 11.01"></polyline>
						</svg>
					</div>
					<h4>Ship Faster</h4>
					<p>Clear roadmap to production</p>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.features-tiled {
		background: var(--color-background);
		overflow: hidden;
	}

	.section-header {
		text-align: center;
		max-width: 900px;
		margin: 0 auto var(--space-16) auto;
	}

	.section-header h2 {
		margin-bottom: var(--space-4);
	}

	.section-intro {
		font-size: var(--text-xl);
		line-height: 1.7;
		color: var(--color-text-secondary);
	}

	.features-grid {
		display: grid;
		grid-template-columns: repeat(4, 1fr);
		grid-template-rows: repeat(3, auto);
		gap: var(--space-6);
	}

	/* Tile Sizes & Positioning */
	.feature-tile.large {
		grid-column: span 2;
		grid-row: span 2;
		padding: var(--space-10);
	}

	.feature-tile.medium {
		grid-column: span 2;
		grid-row: span 1;
		padding: var(--space-8);
	}

	.feature-tile.small {
		grid-column: span 1;
		grid-row: span 1;
		padding: var(--space-6);
		min-height: 180px;
	}

	/* Base Tile Styling */
	.feature-tile {
		background: var(--color-white);
		border: var(--border-width) solid var(--color-border);
		border-radius: var(--border-radius);
		box-shadow: var(--shadow-brutal-sm);
		transition: all var(--transition-fast);
		display: flex;
		flex-direction: column;
		position: relative;
		overflow: hidden;
	}

	.feature-tile:hover {
		transform: translate(2px, 2px);
		box-shadow: 6px 6px 0 var(--color-gray-900);
	}

	/* Pastel Background Variants */
	.tile-purple { background: var(--color-pastel-purple); }
	.tile-blue { background: var(--color-pastel-blue); }
	.tile-green { background: var(--color-pastel-green); }
	.tile-yellow { background: var(--color-pastel-yellow); }
	.tile-pink { background: var(--color-pastel-pink); }

	/* Icon Styles */
	.tile-icon {
		margin-bottom: var(--space-6);
	}

	.icon-circle {
		width: 120px;
		height: 120px;
		border-radius: 50%;
		background: var(--color-white);
		border: var(--border-width) solid var(--color-border);
		display: flex;
		align-items: center;
		justify-content: center;
		box-shadow: var(--shadow-brutal-sm);
	}

	.icon-circle svg {
		color: var(--color-gray-900);
	}

	.tile-icon-small {
		width: 80px;
		height: 80px;
		border-radius: 50%;
		background: var(--color-white);
		border: var(--border-width) solid var(--color-border);
		display: flex;
		align-items: center;
		justify-content: center;
		margin-bottom: var(--space-4);
		box-shadow: var(--shadow-brutal-sm);
	}

	.tile-icon-small svg {
		color: var(--color-gray-900);
	}

	.icon-minimal {
		margin-bottom: var(--space-3);
	}

	.icon-minimal svg {
		color: var(--color-gray-900);
	}

	/* Content Layouts */
	.tile-content h3 {
		font-size: var(--text-2xl);
		margin-bottom: var(--space-3);
		color: var(--color-gray-900);
		font-weight: 800;
	}

	.tile-content p {
		font-size: var(--text-base);
		line-height: 1.6;
		color: var(--color-text-secondary);
		margin-bottom: var(--space-4);
	}

	.tile-content-centered {
		text-align: center;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		height: 100%;
	}

	.tile-content-centered h4 {
		font-size: var(--text-lg);
		margin-bottom: var(--space-2);
		color: var(--color-gray-900);
		font-weight: 700;
	}

	.tile-content-centered p {
		font-size: var(--text-sm);
		color: var(--color-text-secondary);
		margin-bottom: 0;
	}

	/* Feature List */
	.feature-list {
		list-style: none;
		padding: 0;
		margin: 0;
	}

	.feature-list li {
		font-size: var(--text-base);
		color: var(--color-gray-900);
		margin-bottom: var(--space-2);
		font-weight: 600;
		font-family: var(--font-mono);
	}

	/* Stat Highlight */
	.stat-highlight {
		margin-top: var(--space-6);
		padding: var(--space-6);
		background: var(--color-white);
		border-radius: var(--border-radius);
		border: var(--border-width) solid var(--color-border);
		text-align: center;
		box-shadow: var(--shadow-brutal-sm);
	}

	.stat-number {
		font-size: clamp(2rem, 4vw, 3rem);
		font-weight: 900;
		color: var(--color-primary);
		line-height: 1;
		margin-bottom: var(--space-2);
	}

	.stat-label {
		font-size: var(--text-sm);
		color: var(--color-text-secondary);
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.05em;
	}

	/* Responsive Design */
	@media (max-width: 1024px) {
		.features-grid {
			grid-template-columns: repeat(2, 1fr);
		}

		.feature-tile.large,
		.feature-tile.medium {
			grid-column: span 2;
		}

		.feature-tile.small {
			grid-column: span 1;
		}
	}

	@media (max-width: 640px) {
		.features-grid {
			grid-template-columns: 1fr;
			gap: var(--space-4);
		}

		.feature-tile.large,
		.feature-tile.medium,
		.feature-tile.small {
			grid-column: span 1;
			grid-row: span 1;
			padding: var(--space-6);
		}

		.icon-circle {
			width: 80px;
			height: 80px;
		}

		.icon-circle svg {
			width: 40px;
			height: 40px;
		}

		.tile-icon-small {
			width: 60px;
			height: 60px;
		}

		.tile-icon-small svg {
			width: 32px;
			height: 32px;
		}
	}
</style>
