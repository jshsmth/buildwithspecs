---
// No imports needed
---

<section class="section download-section" id="download">
	<div class="container-narrow">
		<div class="download-content">
			<h2>Get the Spec Architect Agent</h2>
			<p class="download-intro">
				Download our free Claude Code agent and start building with specs today. No signup required, no credit card, just better software development.
			</p>

			<div class="download-cta">
				<a href="/downloads/spec-architect.md" download="spec-architect.md" class="btn btn-primary btn-lg download-btn" role="button">
					<svg width="24" height="24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
						<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
						<polyline points="7 10 12 15 17 10"></polyline>
						<line x1="12" y1="15" x2="12" y2="3"></line>
					</svg>
					Download spec-architect.md
				</a>
				<p class="file-info">Free • 29KB • Markdown file</p>
			</div>

			<div class="what-you-get">
				<h3>What's Included</h3>
				<ul>
					<li>✅ Complete spec-architect agent for Claude Code</li>
					<li>✅ Guided Requirements → Design → Tasks workflow</li>
					<li>✅ Built-in templates and best practices</li>
					<li>✅ Quality checklists and approval gates</li>
					<li>✅ Works with any codebase or framework</li>
				</ul>
			</div>

			<div class="installation-snippet">
				<h4>Quick Installation</h4>
				<pre><code># Place in your project
mkdir -p .claude/agents
mv spec-architect.md .claude/agents/

# Ask Claude to use spec mode
# "Let's use spec mode to plan this feature"</code></pre>
			</div>

			<div class="support-note">
				<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
					<path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
				</svg>
				<div>
					<strong>Love this tool?</strong> Payment options coming soon. For now, it's completely free with no restrictions.
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.download-section {
		background: var(--color-primary);
		text-align: center;
		color: white;
	}

	.download-content {
		max-width: 700px;
		margin: 0 auto;
	}

	.download-section h2 {
		color: white;
	}

	.download-intro {
		font-size: var(--text-xl);
		margin-bottom: var(--space-12);
		color: rgba(255, 255, 255, 0.9);
	}

	.download-cta {
		margin: var(--space-12) 0;
	}

	.download-cta .btn {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		gap: var(--space-2);
		font-weight: 700;
		border-radius: var(--border-radius);
		border: var(--border-width) solid var(--color-border);
		cursor: pointer;
		transition: all var(--transition-fast);
		text-decoration: none;
		font-family: var(--font-sans);
		white-space: nowrap;
		position: relative;
	}

	.download-cta .btn-primary {
		background: var(--color-white);
		color: var(--color-primary);
		box-shadow: var(--shadow-brutal-sm);
		border-color: var(--color-border);
	}

	.download-cta .btn-primary:hover {
		transform: translate(2px, 2px);
		box-shadow: 4px 4px 0 var(--color-gray-900);
	}

	.download-cta .btn-primary:active {
		transform: translate(4px, 4px);
		box-shadow: 2px 2px 0 var(--color-gray-900);
	}

	.download-cta .btn-lg {
		padding: var(--space-4) var(--space-8);
		font-size: var(--text-lg);
	}

	.download-btn {
		margin-bottom: var(--space-3);
	}

	.file-info {
		font-size: var(--text-sm);
		color: rgba(255, 255, 255, 0.7);
		margin-bottom: 0;
	}

	.what-you-get {
		background: var(--color-white);
		padding: var(--space-8);
		border-radius: var(--border-radius);
		margin: var(--space-12) 0;
		text-align: left;
		border: var(--border-width) solid var(--color-border);
		box-shadow: var(--shadow-brutal-lg);
	}

	.what-you-get h3 {
		text-align: center;
		margin-bottom: var(--space-6);
		color: var(--color-primary);
	}

	.what-you-get ul {
		list-style: none;
		padding: 0;
		margin: 0;
	}

	.what-you-get li {
		font-size: var(--text-lg);
		padding: var(--space-3) 0;
		color: var(--color-gray-700);
		border-bottom: 1px solid var(--color-gray-200);
	}

	.what-you-get li:last-child {
		border-bottom: none;
	}

	.installation-snippet {
		background: var(--color-white);
		padding: var(--space-8);
		border-radius: var(--border-radius);
		border: var(--border-width) solid var(--color-border);
		text-align: left;
		box-shadow: var(--shadow-brutal-lg);
	}

	.installation-snippet h4 {
		text-align: center;
		margin-bottom: var(--space-6);
		color: var(--color-gray-900);
	}

	.installation-snippet pre {
		margin: 0;
		padding: var(--space-4);
		background: var(--color-gray-900);
		border-radius: var(--border-radius);
		overflow-x: auto;
		border: var(--border-width) solid var(--color-border);
	}

	.installation-snippet code {
		font-size: var(--text-sm);
		line-height: 1.7;
		color: var(--color-gray-100);
		background: transparent;
		border: none;
		padding: 0;
	}

	.support-note {
		margin-top: var(--space-8);
		padding: var(--space-4);
		background: rgba(255, 255, 255, 0.15);
		border-radius: var(--border-radius);
		border: 2px solid rgba(255, 255, 255, 0.3);
		display: flex;
		align-items: center;
		gap: var(--space-3);
		font-size: var(--text-sm);
		color: rgba(255, 255, 255, 0.9);
	}

	.support-note svg {
		color: var(--color-danger);
		flex-shrink: 0;
	}

	.support-note strong {
		color: white;
	}
</style>
