---
---

<section class="section what-is-sdd" id="what-is-sdd">
	<div class="container">
		<div class="sdd-grid">
			<div class="sdd-text">
				<h2>What is Spec-Driven Development?</h2>
				<p class="lead">
					Spec-Driven Development is a software methodology that prioritizes planning and documentation <em>before</em> writing code.
				</p>
				<p>
					Instead of diving straight into implementation, you create a comprehensive specification that guides every aspect of development. Think of it as your blueprint—a living document that evolves with your project.
				</p>

				<div class="comparison-compact">
					<div class="comparison-item bad">
						<strong>❌ Without Specs</strong>
						<p>Start coding → Hit blockers → Rework → Document later</p>
					</div>
					<div class="comparison-item good">
						<strong>✅ With Specs</strong>
						<p>Plan thoroughly → Build confidently → Ship reliably</p>
					</div>
				</div>
			</div>

			<div class="sdd-visual">
				<div class="process-flow">
					<div class="flow-step">
						<div class="step-icon">📋</div>
						<h3>Requirements</h3>
						<p>Define <strong>what</strong> to build</p>
					</div>

					<div class="flow-arrow">↓</div>

					<div class="flow-step">
						<div class="step-icon">🏗️</div>
						<h3>Design</h3>
						<p>Plan <strong>how</strong> to build it</p>
					</div>

					<div class="flow-arrow">↓</div>

					<div class="flow-step">
						<div class="step-icon">📝</div>
						<h3>Tasks</h3>
						<p>Break into <strong>steps</strong></p>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.what-is-sdd {
		background: var(--color-pastel-green);
	}

	.sdd-grid {
		display: grid;
		grid-template-columns: 1fr 1.2fr;
		gap: var(--space-16);
		align-items: center;
	}

	.sdd-text h2 {
		margin-bottom: var(--space-6);
	}

	.lead {
		font-size: var(--text-xl);
		line-height: 1.7;
		margin-bottom: var(--space-6);
		color: var(--color-text-primary);
		font-weight: 500;
	}

	.sdd-text p {
		font-size: var(--text-lg);
		line-height: 1.8;
		margin-bottom: var(--space-6);
	}

	.comparison-compact {
		display: flex;
		flex-direction: column;
		gap: var(--space-4);
		margin-top: var(--space-8);
	}

	.comparison-item {
		padding: var(--space-5);
		border-radius: var(--border-radius);
		border: var(--border-width) solid var(--color-border);
		box-shadow: var(--shadow-brutal-sm);
	}

	.comparison-item.bad {
		background: var(--color-pastel-pink);
	}

	.comparison-item.good {
		background: var(--color-pastel-yellow);
	}

	.comparison-item strong {
		display: block;
		margin-bottom: var(--space-2);
		font-size: var(--text-lg);
	}

	.comparison-item p {
		margin-bottom: 0;
		font-size: var(--text-base);
		color: var(--color-gray-700);
	}

	.process-flow {
		background: var(--color-white);
		padding: var(--space-10);
		border-radius: var(--border-radius);
		border: var(--border-width) solid var(--color-border);
		box-shadow: var(--shadow-brutal);
	}

	.flow-step {
		background: var(--color-pastel-blue);
		padding: var(--space-6);
		border-radius: var(--border-radius);
		text-align: center;
		border: var(--border-width) solid var(--color-border);
		box-shadow: var(--shadow-brutal-sm);
	}

	.step-icon {
		font-size: 3rem;
		margin-bottom: var(--space-3);
	}

	.flow-step h3 {
		font-size: var(--text-xl);
		margin-bottom: var(--space-2);
		color: var(--color-primary);
	}

	.flow-step p {
		font-size: var(--text-base);
		margin-bottom: 0;
		color: var(--color-text-secondary);
	}

	.flow-arrow {
		text-align: center;
		font-size: var(--text-4xl);
		color: var(--color-gray-900);
		margin: var(--space-4) 0;
		font-weight: 700;
	}

	@media (max-width: 968px) {
		.sdd-grid {
			grid-template-columns: 1fr;
			gap: var(--space-12);
		}
	}
</style>
